import React from 'react';
import HeaderMain from '../HeaderMain/HeaderMain';

const Header = () => {
    return (
        <section>
            <HeaderMain/>
        </section>
    );
};

export default Header;